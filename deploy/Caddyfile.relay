{
    key_type rsa2048
    servers {
        protocols h1 h2
    }
}

{$HAOLEME_RELAY_DOMAIN} {
    encode zstd gzip
    route {
        @websocket path /ws/*
        reverse_proxy @websocket relay:8001
        reverse_proxy relay:8000
    }
}
